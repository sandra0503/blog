@import url('https://fonts.googleapis.com/css2?family=Cormorant:wght@400;600;700&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;600&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&family=Work+Sans:wght@400;600&display=swap');

@tailwind base;
@tailwind components;

@css {
  body, html, #app {
    @apply .text-base .font-sans;
    height: 100%;
  }

  h1 {
    @apply .text-2xl .font-serif;
  }

  h2 {
    @apply .text-xl .font-serif;
  }

  .btn {
    @apply .text-base .lowercase .p-2 .m-2 .font-sans;
    transition: background, color .5s;
  }

  .btn-secondary {
    @apply .text-base .btn .bg-black .text-white .font-sans;
  }

  .form-input {
    @apply .flex .flex-col .mx-auto .my-4;
  }

  .input-label {
    @apply .flex-initial .pr-8 .w-full .text-left;
  }

  .input-field,
  input,
  input:-webkit-autofill {
    @apply .flex-1 .h-10 .py-3 .bg-white .w-full;
    border-bottom: 1px solid #e6e6e6;
    background-color: transparent !important;
    -webkit-box-shadow:0 0 0 50px white inset;
  }

  .input-field:focus {
    @apply .border-black;
    outline: none;
  }

  @media (min-width: 768px) {
    body, html, #app {
      @apply .text-base  .font-sans;
    }

    h1 {
      @apply .text-3xl;
    }

    h2 {
      @apply .text-2xl;
    }


    .btn:hover {
      @apply .bg-black .text-white;
    }

    .btn-secondary:hover {
      @apply .bg-black;
    }

    .form-input {
      @apply .flex-row;
    }

    .input-label {
      @apply .w-24;
    }

    .input-field {
      @apply .w-auto .px-3;
    }
  }
}

@tailwind utilities;
